// function = assistant
#include<stdio.h>
/*
void cleaner(int p, int q){
    printf("%d is greater than %d\n", p, q);
}

int main()
{
    int a, b, c, d;
    printf("a : ");
    scanf("%d", &a);    // 10
    printf("b : ");
    scanf("%d", &b);    // 8
    if (a > b){
        cleaner(a, b);      // function call
    }
    printf("c : ");
    scanf("%d", &c);    // 6
    if (a > c){
        cleaner(a, c);      // function call
    }
    printf("d : ");
    scanf("%d", &d);    // 4
    if (a > d){
        cleaner(a, d);      // function call
    }
    return 0;
}
*/

/*
void average(int a, int b){
    printf("average = %.2f\n", (a + b)/2.0);
    // printf("average = %.2f\n", ((float) a + b)/2);
    // printf("average = %.2f\n", (a +(float) b)/2);
    // printf("average = %.2f\n", (float)(a + b)/2);
}

int main(){
    // average(10, 20);
    // printf("Nice work!\n");
    // average(30, 40);
    // average(40);
    int x, y;
    printf("x: ");
    scanf("%d", &x);    // 50
    printf("y: ");
    scanf("%d", &y);    // 60
    average(x, y);
    return 0;
}
*/